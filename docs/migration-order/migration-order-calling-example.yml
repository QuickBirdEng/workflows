name: JS Sanity Requirements

on:
  pull_request:
    branches:
      - main
      - zuerich
    paths:
      - "web/prisma/migrations/**"

jobs:
  PrismaJS:
    uses: QuickBirdEng/workflows/.github/workflows/migration-order.yml@main
    with:
      migrations-dir: "web/prisma/migrations"
    secrets: inherit
