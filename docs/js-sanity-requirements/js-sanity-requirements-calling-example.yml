name: JS Sanity Requirements

on:
  pull_request:
    branches:
      - main
    paths:
      - "prisma/migrations/**"

jobs:
  PrismaJS:
    uses: QuickBirdEng/workflows/.github/workflows/js-sanity-requirements.yml@main
    with:
      migrations-dir: "prisma/migrations"
